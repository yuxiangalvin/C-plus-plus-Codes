#include "Practice.h"

#include <iostream>
#include <string>
#include <vector>
#include <memory>

using namespace std;

/// Write this function to return a vector of all of the
/// ints in nums that are divisible by all of the divisors
/// Return vector must be in ascending order
vector<int> divisibleByAll(vector<int> nums, vector<int> divisors){

    // [YOUR CODE HERE]

}

/// Write factorial to take the factorial of n and return it
/// Remember that factorial(n) means (n) * (n-1) * (n-2) * ... * 1
int factorial(int n){

    // [YOUR CODE HERE]

}

/// Write vectorizeInt to turn an integer into a vector of digits.
/// Examples:
/// Input: 108
/// Output: {1, 0, 8}
/// Input: 55594
/// Output: {5, 5, 5, 9, 4}
/// Input: 3
/// Output {3}
/// One strategy may be to start by creating a vector that gets filled in in reverse order.
/// There is a function in the standard library called reverse that would work like this for a vector:
/// vector<int> v;
/// reverse(v.begin(), v.end());
vector<int> vectorizeInt(int num){

    // [YOUR CODE HERE]

}




